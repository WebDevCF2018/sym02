{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur notre site{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Accueil ✅</h1>

    Voici nos articles:
    <ul>
        <li>Back to homepage</li>
        <li><a href="{{ path("debugAccueilController") }}">Debug to AccueilController.php</a></li>
    </ul>
    {% for item in NosArticles %}
        <h2>{{ item.getTitre }}</h2>
        <h3>Le {{ item.getTemps|date("d/m/Y") }} par {{ item.getAuteur }}</h3>
        <p>{{ item.getDescription|slice(0,150) }}</p><hr>
    {% endfor %}
</div>
{% endblock %}
